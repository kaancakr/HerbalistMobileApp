import Constants from "expo-constants";
import * as Notifications from "expo-notifications";
import React from "react";
import {
  StyleSheet,
} from "react-native";


export default function SendNotifications({ navigation }) {

}

const styles = StyleSheet.create({
  
});
